---
executions:
  - name: "tests-calico"
    env:
      - ARTIFACTS_ARCHIVE=true
    root: ./tests_calico/
    timeout: 3600
    cluster-count: 1
    cluster-selector:
      - packet
    cluster-env:
      - KUBECONFIG
