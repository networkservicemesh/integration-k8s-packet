---
executions:
  - name: "tests"
    env:
      - ARTIFACTS_ARCHIVE=true
    root: ./tests_default/
    timeout: 3600
    cluster-count: 1
    cluster-selector:
      - packet
    cluster-env:
      - KUBECONFIG
